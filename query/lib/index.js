const {PostCreated} = require('./postCreated');
const {CommentCreated} = require('./commentCreated');
const {CommentUpdated} = require('./commentUpdated');

module.exports = {
    PostCreated,
    CommentCreated,
    CommentUpdated
}
